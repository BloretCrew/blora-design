# Fieldset migration

Keep the native \`fieldset\` and \`legend\` semantics. Replace v1 styling hooks with the same formal 2.0 classes:

\`\`\`html
<fieldset class="blora-fieldset">
  <legend class="blora-fieldset__legend">发布设置</legend>
  <p class="blora-fieldset__description">组织一组相关控件。</p>
</fieldset>
\`\`\`

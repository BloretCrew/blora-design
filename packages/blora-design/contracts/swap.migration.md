# Swap migration

Replace v1 \`.blora-swap\` authored internals with the official declarative element:

\`\`\`html
<blora-swap name="theme" off-label="深色模式" on-label="浅色模式"></blora-swap>
\`\`\`

The element owns the native checkbox, accessible focus behavior, state labels, and Lucide icons.

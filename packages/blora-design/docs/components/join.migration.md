# Join

New in 2.0. Groups adjacent controls so they share one outline. Button-only groups can still use `.blora-button-group`.

```html
<div class="blora-join">
  <input class="blora-input" type="search" />
  <button class="blora-button" type="button" data-variant="primary">搜索</button>
</div>
```
